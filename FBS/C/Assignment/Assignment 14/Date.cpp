#include <stdio.h>

struct Time {
    int hour, minute, sec;
};

void store(struct Date *t) {
    printf("Enter hour, minute, sec:\n");
    scanf("%d %d %d", &d->hour, &d->minute, &d->sec);
}

void display(struct Date t) {
    printf("Date: %d-%d-%d\n", d.hour, d.minute, d.sec);
}

 main() {
    struct Time= t1;
    store(&t1);
    display(t);
}


